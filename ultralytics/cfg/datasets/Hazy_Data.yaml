# Ultralytics YOLO 🚀, AGPL-3.0 license
# Hazy Dataset Configuration
# Documentation: https://docs.ultralytics.com/datasets/detect/
# Example usage: yolo train data=Hazy_Data.yaml

# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: /userhome/lhf/Github/ultralytics/dataset  # dataset root dir
train: train  # train images (relative to 'path')
val: val  # val images (relative to 'path')
test: test  # test images (relative to 'path')

depth: Depth  # depth images (relative to 'path')


# Classes (Vehicle detection classes)
names:
  0: car
  1: truck
  2: bus
